# ============================================================================
# Smartphone Intelligence Platform - Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control
# ============================================================================
#
# PRODUCTION DEPLOYMENT NOTES:
# - Use environment variables or secrets management (e.g., AWS Secrets Manager,
#   HashiCorp Vault, Kubernetes Secrets) instead of .env files in production
# - Never commit .env files to version control
# - Rotate credentials regularly
# - Use least-privilege access for database users
# ============================================================================

# ----------------------------------------------------------------------------
# MySQL Database Configuration (Optional - for data pipelines)
# ----------------------------------------------------------------------------
# Note: MySQL is optional. The API uses Snowflake as the primary data source.
# MySQL is only needed if running data ingestion pipelines.
MYSQL_HOST=localhost
MYSQL_PORT=3307
MYSQL_DB=smartphone_intelligence
MYSQL_USER=app_user
MYSQL_PASSWORD=your_mysql_password_here

# ----------------------------------------------------------------------------
# Snowflake Configuration (REQUIRED for API)
# ----------------------------------------------------------------------------
# Account identifier format: ORG-ACCOUNT (e.g., MOPRGFY-EE19523)
# or ACCOUNT.REGION.CLOUD (e.g., LX83893.ap-southeast-1.aws)
# or ACCOUNT.REGION (e.g., LX83893.ap-southeast-1)
SNOWFLAKE_ACCOUNT=your_snowflake_account_here
SNOWFLAKE_USER=your_snowflake_username
SNOWFLAKE_PASSWORD=your_snowflake_password_here
SNOWFLAKE_WAREHOUSE=your_warehouse_name
SNOWFLAKE_DATABASE=your_database_name
SNOWFLAKE_SCHEMA=PUBLIC

# Optional: Snowflake role (leave empty or omit to use default role)
# If your user has a default role, you can leave this empty
SNOWFLAKE_ROLE=

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
# FastAPI server port (default: 8000)
# For production, set via environment variable or gunicorn.conf.py
PORT=8000

# API host (default: 0.0.0.0 for production, localhost for development)
# Use 0.0.0.0 to bind to all interfaces (required for Docker/containers)
API_HOST=0.0.0.0

# Environment: development or production
# Controls auto-reload and debug features
ENVIRONMENT=production

# API base URL for Streamlit dashboard
# For production, set to your deployed API URL (e.g., https://api.example.com)
# For local development, use http://localhost:8000
API_URL=http://localhost:8000

# ----------------------------------------------------------------------------
# Gunicorn Configuration (Production Server)
# ----------------------------------------------------------------------------
# Number of worker processes (default: CPU count * 2 + 1)
# Adjust based on your server resources
GUNICORN_WORKERS=

# Gunicorn log files (use "-" for stdout/stderr, or file paths)
GUNICORN_ACCESS_LOG=-
GUNICORN_ERROR_LOG=-
GUNICORN_LOG_LEVEL=info

# Gunicorn PID file (optional, for process management)
# GUNICORN_PIDFILE=/var/run/smartphone-intelligence-api.pid
